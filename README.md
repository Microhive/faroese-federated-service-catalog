# faroese-federated-service-catalog
A Git-backed, federated catalog of Faroese public services (and later business services) modeled in CPSV-AP (JSON-LD). Supports central crawling/aggregation, duplicate detection, and process/compliance validation (SLA/DSA).
